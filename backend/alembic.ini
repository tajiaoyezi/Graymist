[alembic]
script_location = migrations
prepend_sys_path = .
# Production: override with a SYNC PostgreSQL driver URL (e.g. postgresql+psycopg://...)
# or set env var ALEMBIC_SQLALCHEMY_URL. Empty => falls back to app.config settings.
# Note: asyncpg is async-only and cannot be used for alembic's sync migrations.
sqlalchemy.url =

[post_write_hooks]
